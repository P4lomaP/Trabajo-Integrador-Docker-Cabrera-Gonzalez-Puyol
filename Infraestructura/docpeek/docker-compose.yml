services:
  dozzle:
    image: amir20/dozzle:latest
    container_name: docpeek-visualizer
    restart: unless-stopped
    ports:
      - '8084:8080' # <--- Cambiamos 8083 por 8084
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
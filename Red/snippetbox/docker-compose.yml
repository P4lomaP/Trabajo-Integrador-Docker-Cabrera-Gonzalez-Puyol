version: '3.8'

services:
  snippet-box:
    image: pawelmalak/snippet-box:latest
    container_name: euge_snippetbox
    ports:
      - "8088:5000"
    volumes:
      - ./snippetbox-data:/app/data
    restart: unless-stopped